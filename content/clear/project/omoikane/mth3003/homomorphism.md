# Homomorphism

A **homomorphism** is a map \theta: G \to H between two groups that preserves the group operation: $\boxed{\theta(g_1 g_2) = \theta(g_1)\theta(g_2)}$ (where the operation on the left is in G, and on the right is in H).
